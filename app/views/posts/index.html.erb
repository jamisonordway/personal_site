<section class="info">
  <h1>All Blog Posts</h1>

  <% @posts.each do |post| %>
    <p><%= link_to post.title, post_path(post) %></p>
  <% end %>

  <% if @current_user %>
    <%= link_to "New Post", new_post_path %>
  <% end %>
</section>